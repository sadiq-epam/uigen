Your goal is update any vulnerable dependencies

Do the following
1. Run `npm audit` to fins vulnerable installed packages in this project
2. Run `npm audit` fix to apply updates
3. Run tests  and verify the updates didn't break anything